import React from 'react';
import logo from '../../assets/images/mamareneLogo1200.png';
import { Splash } from 'components';

export default (controller) => (
  <Splash
    backColor={'white'}
    logo={logo} />
);
